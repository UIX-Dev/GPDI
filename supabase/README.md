# 아카이브 — 더 이상 사이트에서 쓰지 않음

2026-08-02부로 Supabase 의존을 제거하고 보도자료·교육영상을 정적 HTML로 전환했다.
이 폴더는 **데이터 원본 기록**으로만 남긴다. 실행 코드는 어디서도 참조하지 않는다.

- `01_setup.sql` — 테이블 2개 + 인덱스 + RLS 정책 (참고용)
- `02_seed.sql` — 보도자료 51건 + 교육영상 17건 원본 데이터

## 지금 화면에 나가는 데이터의 위치

| 페이지 | 위치 |
|---|---|
| 보도자료 전체 51건 | `press.html` 의 `#pp-list` |
| 교육영상 전체 17건 | `education.html` 의 `#ed-grid` |
| 홈 보도자료 12건 | `index.html` 의 `#landing-press-list` |
| 홈 교육영상 6건 | `index.html` 의 `#landing-video-list` |

항목을 추가할 때는 위 HTML에 `<li>` 를 직접 넣는다. 정렬은 최신순
(`published_at` 내림차순, 같은 날짜면 나중에 등록된 것이 위).

이 시드는 2026-05-01 기준이며, 그 이후 어드민으로 추가된 항목은 포함되어 있지 않을 수 있다.
어드민 UI 원본은 `_백업/supabase-백엔드_2026-08-02/` 에 보관되어 있다.
